/** Contact email shown when form is unavailable */
export const CONTACT_EMAIL = "rioredwards@gmail.com";

/** Locale for date formatting */
export const DEFAULT_LOCALE = "en-US";

/** Words per minute for reading time calculation */
export const READING_SPEED_WORDS_PER_MIN = 200;
