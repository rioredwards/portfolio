/* This is a collection of parameters to allow for easy adjustments in the UI */
import { BG_GRADIENT_COLORS } from './colors';
import { AVATAR_SIZES, AVATAR_TOP_MARGINS } from './sizes';

const params = {
  BG_GRADIENT_COLORS,
  AVATAR_SIZES,
  AVATAR_TOP_MARGINS,
};

export default params;
